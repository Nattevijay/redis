spring:
  application:
    name: redis_app
  datasource:
    url: jdbc:mysql://localhost:3306/redis_app?useSSL=false&serverTimezone=UTC
    username: root
    password: 21082001@Vijay
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update


  data:
    redis:
      host: localhost
      port: 6379

  cache:
    type: redis